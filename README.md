
# SMPM Fullstack (Vue 3 + Node/Express + MongoDB + JWT)

Seeds automáticos:
- Usuário: `admin@smpm.com` / `123456`
- 3 manutenções exemplo

## API
- POST `/auth/register`
- POST `/auth/login`
- GET `/api/maintenance`
- GET `/api/maintenance/:id`
- POST `/api/maintenance`
- PUT `/api/maintenance/:id`
- DELETE `/api/maintenance/:id`
